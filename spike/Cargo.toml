[package]
name = "claims-spike"
version = "0.1.0"
edition = "2021"
publish = false

# Throwaway spike proving design/CLAIMS_ENGINE_DOMAIN_MODEL.md (2026-07-02).
# Deliberately isolated from the root workspace: own lock file, own deps.
[workspace]

[dependencies]
serde_json = "1"
sha2 = "0.10"
hex = "0.4"
thiserror = "2"
sophia = { version = "0.9", features = ["jsonld"] }
jsonschema = "0.33"
